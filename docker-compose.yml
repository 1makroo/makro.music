services:
  makro-music:
    build: .
    container_name: makro-music
    restart: unless-stopped
    env_file:
      - .env
    environment:
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      MAX_PLAYLIST_ITEMS: ${MAX_PLAYLIST_ITEMS:-50}
    init: true
